[package]
name = "pdfium-wrapper-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to pdfium_wrapper — a minimal C wrapper around PDFium for PDF rasterization"
license = "MIT OR Apache-2.0"
links = "pdfium_wrapper"

[build-dependencies]
