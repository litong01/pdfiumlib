/*
 * Minimal math.h for iOS cross-compilation from Linux.
 * Only declares the functions used by pdfium_wrapper.c.
 */
#ifndef _CROSS_MATH_H
#define _CROSS_MATH_H

extern double ceil(double __x);

#endif
